apiVersion: v1
kind: Service
metadata:
  name: {{ include "calculatorback-jorge.fullname" . }}
  labels:
    {{- include "calculatorback-jorge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calculatorback-jorge.selectorLabels" . | nindent 4 }}
